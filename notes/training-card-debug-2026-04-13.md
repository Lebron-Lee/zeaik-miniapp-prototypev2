## 培训介绍卡片未更新排查记录

- 当前预览入口 `/` 实际由 `MiniAppShell -> HomePage` 渲染。
- 重启前端服务后，首页默认仍停留在普通首页态，并未直接进入培训对话流。
- 浏览器中可见两个与培训相关的入口：
  - 左侧对话记录中的“茶水区服务标准培训 今天 09:41”按钮。
  - 首页功能卡片中的“9.9元餐饮AI培训”按钮（id: `zeaik-home-training-entry`）。
- 点击首页功能卡片中的培训入口后，当前视图没有切换，说明用户截图更可能命中的是“对话记录”中的培训会话入口，而不是首页功能卡片入口。
- 因此下一步需要优先验证左侧对话记录中的培训会话按钮进入后的真实渲染内容，再决定是修正 HomePage 的 training conversation 分支，还是修正另一处分支。
